/**
 * Единая точка загрузки модулей и их инициализации для всего проекта.
 */

$(document).ready(function() {
    webtlo.init().then(() => {
        console.log('Все модули инициализированы');
    });


    // Проверим наличие новой версии
    checkNewVersion();
});
